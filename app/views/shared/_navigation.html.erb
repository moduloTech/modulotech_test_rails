<nav class="navbar navbar-expand-lg mb-4">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>">MtBnb</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <!-- TODO: Implement this search -->

      <%= form_with url: rooms_path, method: :get, id: 'navbarSupportedContent', class: 'collapse navbar-collapse' do |form| %>
        <%= form.label :class => 'form-label' do %>
          Where <%= form.text_field :location, :class => 'form-control', value: params[:location], placeholder: "Enter city" %>
        <% end %>
        <%= form.label :class => 'form-label' do %>
          Check in <%= form.date_field :start_date, :class => 'form-control', value: params[:start_date] %>
        <% end %>
        <%= form.label :class => 'form-label' do %>
          Check out <%= form.date_field :end_date, :class => 'form-control', value: params[:end_date] %>
        <% end %>
        <%= form.submit "Search", :class => 'btn btn-primary' %>
      <% end %>

      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li class="nav-item dropdown">
            <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown" role="button" aria-expanded="false">
              <span><%= current_user.email %></span>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              <li>
                <!-- TODO: Implement route '/my/rooms' to display the rooms owned by the user -->
                <!-- TODO: '/my/rooms' must allow the user to fully manage its rooms -->
                <%= link_to 'My rooms', '/my/rooms', class: 'dropdown-item' %>
              </li>
              <li>
                <!-- TODO: Implement route '/my/reservations' to display the reservations made by the user -->
                <!-- TODO: '/my/rooms' must allow the user to fully manage its reservations -->
                <%= link_to 'My reservations', '/my/reservations', class: 'dropdown-item' %>
              </li>
              <li class="border-top">
                <%= button_to destroy_user_session_path, class: 'dropdown-item', method: :delete do %>
                  Sign out
                <% end %>
              </li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Sign in', new_user_session_path, class: 'btn btn-outline-primary' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
