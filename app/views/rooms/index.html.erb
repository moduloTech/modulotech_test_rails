<h1 class="text-center">ROOMS LIST</h1>

<div class="container my-4">
  <%= turbo_frame_tag "rooms", target: "_top" do %>
    <div class="row">
      <%= render @rooms %>
    </div>

    <div class="mt-4">
      <%== pagy_bootstrap_nav(@pagy, link_extra: "data-turbo-frame='rooms'") %>
    </div>
  <% end %>
</div>
